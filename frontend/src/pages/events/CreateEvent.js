export default function CreateEvent() {
  return <h1>Create Event</h1>;
}

// Functionality: //
// if user is logged in - create new event with all fields.
// if user is not logged in - redirect to auth page
